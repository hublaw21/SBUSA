package com.skaterbayusa.test.mytestappv2a;

import android.content.Intent;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.text.TextUtils;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;

public class NewElementActivity extends AppCompatActivity {
/*
    public static final String EXTRA_REPLY = "com.example.android.elementlistsql.REPLY";

    private EditText mEditElementIDView;
    private EditText mEditElementNameView;


    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_new_element);
        mEditElementIDView = findViewById(R.id.edit_elementID);
        mEditElementNameView = findViewById(R.id.edit_elementName);

        final Button button = findViewById(R.id.button_save);
        button.setOnClickListener(new View.OnClickListener() {
            public void onClick(View view) {
                Intent replyIntent = new Intent();
                if (TextUtils.isEmpty(mEditElementIDView.getText())) {
                    setResult(RESULT_CANCELED, replyIntent);
                } else {
                    String element = mEditElementIDView.getText().toString();
                    replyIntent.putExtra(EXTRA_REPLY, element);
                    setResult(RESULT_OK, replyIntent);
                }
                finish();
            }
        });
    }
    */
}
