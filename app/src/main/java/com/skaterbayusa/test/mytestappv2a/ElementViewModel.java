package com.skaterbayusa.test.mytestappv2a;

import java.util.List;
/*
public class ElementViewModel extends AndroidViewModel {

    private final LiveData<List<Element>> elementList;

    public ElementViewModel (@NonNull Application application){
        super(application);

        elementList = ElementRoomDatabase
                .getElementDB(getApplication())
                .elementDao()
                .getAllElements();
    }

    public LiveData<List<Element>> getElementList() {
        return elementList;
    }

    /*
    private ElementRepository mRepository;

    private LiveData<List<Element>> mAllElements;

    public ElementViewModel (Application application) {
        super(application);
        mRepository = new ElementRepository(application);
        mAllElements = mRepository.getAllElements();
    }

    LiveData<List<Element>> getAllElements() { return mAllElements; }

    public void insert(Element element) { mRepository.insert(element); }
    */
/*
}
*/
